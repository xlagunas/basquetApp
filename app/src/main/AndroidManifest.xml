<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cat.xlagunas.basquetapp" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="cat.xlagunas.basquetapp.CustomApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name="cat.xlagunas.basquetapp.ui.activity.SelectionActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="cat.xlagunas.basquetapp.ui.activity.FavoriteSelectionActivity"
            android:label="@string/title_activity_favorite_selection" >
        </activity>

        <activity
            android:name="cat.xlagunas.basquetapp.ui.activity.LeagueActivity"
            android:label="@string/title_activity_league" >
        </activity>


        <activity
            android:name="cat.xlagunas.basquetapp.ui.activity.CompetitionActivity"
            android:label="@string/title_activity_competition" >
        </activity>

        <service
            android:name="cat.xlagunas.basquetapp.service.ApiService"
            android:exported="false" >
        </service>

        <service
            android:name="cat.xlagunas.basquetapp.service.UpdateService"
            android:exported="false" >
        </service>

        <receiver
            android:name="cat.xlagunas.basquetapp.service.AlarmReceiver"
            android:enabled="true"
            android:exported="false" >
        </receiver>
    </application>

</manifest>
